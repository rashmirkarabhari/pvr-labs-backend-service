# Production profile overrides
logging:
  level:
    root: WARN
    com.pvrlabs.payment: INFO
    com.cashfree: WARN

management:
  endpoint:
    health:
      show-details: never

springdoc:
  swagger-ui:
    enabled: ${SWAGGER_ENABLED:false}
  api-docs:
    enabled: ${SWAGGER_ENABLED:false}
